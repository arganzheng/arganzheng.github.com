---
layout: post
series: product-and-experience
title: "产品与体验（04）：不确定性的呈现与非对话形态"
subtitle: "Presenting Uncertainty and Beyond the Chat Box"
tags: [AI, LLM, AI-Application, Product, UX, Generative UI]
catalog: true
---

上一篇说校准信任的手段都是"让用户看到真实的能力与不确定"。这一篇讲**怎么让用户看到**：引用与来源怎么呈现、置信度怎么表达（不给数字）、拒答怎么说、流式与进度、agent 的步骤、审批的交互、diff 式审阅。然后讲形态：对话框是 2023 年的默认，不是最好的默认——内联建议、按钮化动作、环境感知、生成式 UI、语音与实时、后台交付，各在什么时候比对话框好。

呈现的三个原则：**诚实**（不把不确定的呈现为确定的）、**可核对**（用户能验证）、**可行动**（用户知道下一步能做什么）。它们把前六层的工程机制（引用正确率、拒答出口、审批的 `justification`、步骤流、diff）翻译成用户看到的东西。

本篇要回答的核心问题是：

> **引用、置信度、拒答各怎么呈现？[^q0] 进度、agent 步骤、审批、diff 各怎么呈现？[^q1] 对话框为什么不该是默认形态，非对话形态有哪些、怎么选？[^q2]**

## 一、总览

### 1. 呈现与机制的对应

| 呈现 | 背后的机制 | 层 |
|---|---|---|
| 引用可点开 | 检索的块 id 与来源文档、引用正确率 | L3、L5 |
| 不确定用行为表达 | 拒答出口、自一致性、judge | L2 第三篇、L5 第七篇 |
| 拒答说缺什么 | 结构化输出的出口字段与原因 | L2 第三篇 |
| 首字前的骨架、token 流、步骤流、进度 | 流式事件、运行时的事件流 | L1 第二篇、L4 第三篇、L6 第三篇 |
| agent 步骤可视化 | 会话日志、决策点日志 | L4 第三篇、L5 第六篇 |
| 审批带理由与影响 | `execpolicy` 的 `justification`、权限判定 | L4 第五篇 |
| diff 式审阅 | 变更的前后状态、提案先于生效 | L4 第八篇 |
| 生成式 UI | 结构化输出、MCP Apps | L2 第三篇、L4 第二篇 |

### 2. 本文的章节安排

第二章引用、置信度、拒答；第三章进度、步骤、审批、diff；第四章对话框的局限；第五章非对话形态；第六章选形态的判据；第七章实践建议。

## 二、引用、置信度、拒答

### 1. 引用与来源

每个事实断言能**点开定位到原文**（哪份文档、哪一段，高亮）；引用的粒度到句子或段落而不是"参考文档 A"；引用正确率（L3 第七篇、L5 第三篇）在这里是产品指标——一个指向无关段落的引用比没有引用更伤信任。**没有依据的断言不呈现为事实**（L5 第七篇）：要么去掉，要么明确标为"未找到依据的推断"。来源的可信度也呈现（内部文档 vs 网页 vs 模型自身知识）。

### 2. 置信度：用行为不用数字

不给用户原始的置信分数——用户无法解读"0.73"，且模型的自评不可靠（L5 第七篇）。用**行为**表达确定程度：

| 系统的确定程度 | 呈现 |
|---|---|
| 高（有依据、自一致） | 直接给答案 + 引用 |
| 中（有依据但不完全匹配、或候选间有分歧） | 给几个候选并说差别（"如果你指的是 A 则…，如果是 B 则…"） |
| 低（依据不足） | 问澄清问题，或说"我不确定，以下是能找到的相关信息" |
| 无（没找到） | 拒答，说缺什么、下一步能做什么 |

用语言而不是数字（"我不确定"、"这一点没有找到依据"），并在系统提示里让模型按这个梯子行为（L2 第二篇的出口设计）。

### 3. 拒答

"无法回答"是最差的拒答。好的拒答有三部分：**缺什么**（"你的问题里没有订单号"、"知识库里没有关于 X 的内容"）、**下一步**（"提供订单号，或我帮你查最近的三笔"、"你可以联系 …"）、**已知的部分**（"关于 Y 我找到了这些"）。拒答是产品的正常路径而不是错误页——L2 第三篇的出口字段（原因、缺失信息、建议）就是为这个呈现设计的；拒答率进面板（L3 第七篇、L5 第四篇）。

## 三、进度、步骤、审批、diff

### 1. 首字前与流式

首字前 0.5–1 秒：骨架、状态（"正在检索…"）、**已知信息先渲染**（用户的问题回显、检索到的来源先列出来——来源在回答之前就有）。token 流让用户在第一个 token 就开始读（L6 第三篇的感知延迟）。结构化输出用增量解析边到边渲染（L2 第三篇）。

### 2. agent 步骤的可视化

用户要知道 agent 在做什么、为什么。**分层**：默认一行摘要（"已读取 3 个文件，正在修改 `auth.py`"），可展开到步骤（每步的工具、参数摘要、结果摘要），再展开到完整内容——DeepSeek Harness 的 Trajectory 视图按来源检视每条记录，Codex 与 Claude Code 的步骤流是同一思想的不同密度。**不分层的全量步骤是噪声**，用户会关掉。步骤可视化同时是信任校准（透明）与调试入口（用户能指出哪步错了——反馈带 trace id，L5 第六篇）。

### 3. 进度与预估

长任务给进度（完成 3 / 7 步、已用 2 分钟）与预估（基于历史同类任务的分布——不确定就给范围）；后台 agent 的完成通知与失败通知（部分结果 + 阻塞原因——L4 第九篇）。

### 4. 审批的交互

审批请求要让人在几秒内判断：**为什么问**（`justification`——"这条命令会删除目录"）、**影响范围**（会改哪些文件、会调哪个外部系统、能不能撤销）、**选项**（允许一次 / 允许本会话这类 / 拒绝 / 让我看细节——L4 第五篇的批一类解审批疲劳）、**默认安全**（超时不批 = 拒绝）。审批不该是一个"允许 / 拒绝"的弹窗——那会训练用户点允许。

### 5. diff 式审阅

变更以前后对比呈现（代码 diff、文档的修订标记、配置的字段级对比、数据的行级对比），人能**部分接受**（接受这三处、拒绝那一处）、能评论、能要求修改。整段替换（"这是新版本"）让人无法核对——第三篇的可核对呈现。PR 是软件领域的成熟形态；文档、表格、业务提案要有各自的 diff 形态。

## 四、对话框的局限

对话框在 2023 年成为默认，因为它是把模型接进产品最便宜的方式。它的局限：

- **用户要会问**：空白输入框把设计的负担转给了用户（"我该输入什么？"）；边界外的问题最容易在这里出现（第一篇）。
- **打断工作流**：用户在编辑器 / 表格 / 邮件里，要切到另一个窗口问、再复制回来。
- **输出是一段文字**：表格、图表、表单、可点的动作被压成文本。
- **来回成本**：多轮澄清对简单任务是负担。
- **不适合长任务**：用户盯着对话框等 agent 跑（第二篇的同步 vs 后台）。

对话框适合的：探索性、需要来回的、用户知道要问什么的任务。其他的有更好的形态。

## 五、非对话形态

| 形态 | 是什么 | 适合 | 代表 |
|---|---|---|---|
| **内联建议** | 在用户正在编辑的地方直接给（补全、改写建议、灰字） | 高频、粒度小、用户在流中 | 代码补全、邮件补全、表格公式建议 |
| **按钮化动作** | 一次点击触发预定义的任务（"总结"、"翻译"、"修复这个错误"、"生成测试"） | 任务明确、参数少、用户不该写 prompt | 编辑器右键菜单、邮件的"起草回复" |
| **环境感知** | AI 在用户的工作流里而不是另一个窗口：IDE、邮件客户端、表格、CRM、IM 内 | 几乎所有生产力场景 | Copilot 在 IDE、AI 在 Slack / 飞书里（OpenHarness 的 ohmo——L4 第六篇） |
| **生成式 UI** | 模型输出结构化数据，前端渲染成表格 / 图表 / 表单 / 卡片而不是一段文字 | 输出有结构（对比、列表、数据、可选项） | 购物比较卡片、数据分析的图表、表单预填；MCP Apps 是协议层版本（L4 第二篇） |
| **语音与实时** | 语音输入输出、低延迟的来回（Realtime API，L1 第二篇） | 免手、移动、客服电话、陪练 | 语音助手、电话客服 |
| **后台交付** | 提任务、走开、收到 PR / 草稿 / 报告 / 通知 | 多步、有落点、可等（第二篇） | 后台 coding agent、定时报告 |
| **环境自动** | 无界面：事件触发、AI 处理、结果进系统（分类、路由、抽取） | 边界内、可验证、高量（第二篇的自动化） | 工单分类、表单抽取 |

多数好的 AI 产品是这些形态的组合，对话框只是其中一个入口（Cursor 的四种形态——第二篇）。

## 六、选形态的判据

| 问 | 倾向 |
|---|---|
| 用户在哪？ | 在工作流里 → 内联 / 环境感知；不在 → 对话或后台 |
| 任务多长？ | 秒级 → 内联 / 按钮；分钟 → 同步 agent；更长 → 后台 |
| 输出是什么形状？ | 文本 → 对话 / 内联；结构 → 生成式 UI；变更 → diff |
| 要不要来回？ | 要 → 对话；不要 → 按钮 / 后台 |
| 参数用户知道吗？ | 知道且少 → 按钮；不知道 / 探索 → 对话 |
| 手与眼空吗？ | 不空 → 语音 |
| 输出要人看吗？ | 不用（边界内可验证）→ 环境自动 |

一条纪律：**先问"为什么是对话框"**——如果答案是"最容易做"，多半有更好的形态。

## 七、实践建议

1. **审引用**：能点开定位到段落吗？引用正确率多少？无依据的断言标了吗？
2. **置信度用行为梯子**：直接给 / 候选 / 澄清 / 拒答——写进系统提示与出口 schema；去掉一切原始分数。
3. **重写拒答文案**：缺什么、下一步、已知部分。
4. **步骤分层**：摘要 → 步骤 → 完整；进度与预估；后台的通知含部分结果。
5. **审批三要素**：理由、影响、批一类的选项；超时即拒。
6. **变更全部 diff 化**，支持部分接受。
7. **对每个对话框问"为什么是对话框"**，找一个该内联化 / 按钮化 / 生成式 UI 化 / 后台化的功能。

## 八、本文小结

- 三原则：诚实、可核对、可行动；呈现是前六层机制的翻译。
- 引用可点开到段落、正确率是产品指标、无依据不呈现为事实；置信度用行为梯子（直接给 / 候选 / 澄清 / 拒答）不用数字；拒答三部分（缺什么、下一步、已知部分），是正常路径。
- 首字前骨架与已知信息先渲染、token 流、增量解析；agent 步骤分层（摘要 → 步骤 → 完整，DeepSeek Harness Trajectory、Codex / Claude Code 步骤流），不分层是噪声；进度与预估；审批三要素 + 批一类 + 超时即拒；diff 式审阅支持部分接受。
- 对话框的五个局限（用户要会问、打断工作流、输出压成文本、来回成本、不适合长任务），适合探索与来回。
- 非对话形态七种（内联、按钮、环境感知、生成式 UI、语音实时、后台交付、环境自动），多数产品是组合；选形态按用户在哪、任务多长、输出形状、要不要来回、参数是否已知、手眼是否空、要不要人看；先问"为什么是对话框"。

## 九、自测

1. 一个 RAG 产品在每条回答末尾列"参考文档：A、B、C"。按本文哪里不够？改成什么？

   <details markdown="1"><summary>答案</summary>
   引用粒度到文档不到段落，用户无法核对具体断言的依据；也没区分哪些断言有依据、哪些是推断。改：每个事实断言旁的引用可点开定位到具体段落并高亮；无依据的断言标为推断或去掉；引用正确率进指标；来源可信度（内部文档 / 网页）可见。详见[第二章](#二引用置信度拒答)。
   </details>

2. 团队想在每条回答旁显示"置信度 87%"。为什么不该？替代是什么？

   <details markdown="1"><summary>答案</summary>
   用户无法解读数字，且模型自评的置信度不可靠（L5 第七篇）——87% 可能对应任何真实正确率，反而制造虚假的精确感。替代：用行为表达——确定时直接给 + 引用，中等时给候选并说差别，低时问澄清或说"我不确定"，无时拒答说缺什么；用语言不用数字。详见[第二章](#二引用置信度拒答)。
   </details>

3. 一个 coding agent 默认展开全部步骤（每次工具调用的完整参数与返回），用户反馈"太乱关掉了"。怎么改？

   <details markdown="1"><summary>答案</summary>
   分层：默认一行摘要（做到哪一步、正在做什么），可展开到步骤列表（工具名 + 参数摘要 + 结果摘要），再展开到完整内容；关键事件（审批请求、错误、完成）主动浮现，其余折叠。透明要可控——不分层的全量是噪声。详见[第三章](#三进度步骤审批diff)。
   </details>

4. 审批弹窗只有"允许 / 拒绝"两个按钮，用户点允许的比例 99%。问题与改法？

   <details markdown="1"><summary>答案</summary>
   没有理由与影响范围，用户无法判断只能点允许（审批疲劳 + 过度信任）；99% 说明审批已失效。改：显示为什么问（`justification`）、影响范围（改哪些文件、调哪个系统、可否撤销）、选项加"允许本会话这类"与"看细节"、超时即拒；同时按 L4 第五篇的风险分级减少不必要的审批，只问真正需要判断的。详见[第三章](#三进度步骤审批diff)。
   </details>

5. 一个"AI 比较三款产品"的功能输出一段 400 字的文字。用第五章判断该是什么形态。

   <details markdown="1"><summary>答案</summary>
   输出有结构（三个对象 × 若干维度）→ 生成式 UI：模型输出结构化数据（L2 第三篇），前端渲染成对比表格或卡片，维度可排序、可点开来源；参数少且明确（三款产品、几个维度）→ 可按钮化触发而不是让用户写 prompt。一段文字把结构压平了，用户还要自己在脑子里建表。详见[第五章](#五非对话形态)、[第六章](#六选形态的判据)。
   </details>

## 下一篇

[产品指标与回流](/ai-product-metrics-and-feeding-back-into-the-system.html)

[^q0]: 引用：每个事实断言可点开定位到原文段落并高亮，粒度到句段而非"参考文档 A"，引用正确率是产品指标（指向无关段落比没有更伤信任），无依据的断言不呈现为事实（去掉或标为推断），来源可信度可见。置信度不给原始分数（用户无法解读、模型自评不可靠），用行为梯子表达：高——直接给 + 引用；中——给候选并说差别；低——问澄清或"我不确定，以下是相关信息"；无——拒答；用语言不用数字，系统提示与出口 schema 让模型按梯子行为。拒答三部分：缺什么（"没有订单号"、"知识库无 X"）、下一步（提供信息、替代操作、联系人）、已知的部分；拒答是正常路径不是错误页，出口字段为它设计，拒答率进面板。详见[第二章](#二引用置信度拒答)。

[^q1]: 首字前 0.5–1 秒用骨架、状态、已知信息先渲染（问题回显、来源先列），token 流让第一个 token 就开始读，结构化输出增量解析边到边渲染。agent 步骤分层：默认一行摘要 → 可展开步骤（工具、参数摘要、结果摘要）→ 完整内容（DeepSeek Harness Trajectory 按来源检视、Codex 与 Claude Code 步骤流），关键事件主动浮现，不分层的全量是噪声；步骤可视化同时是信任校准与调试入口（反馈带 trace id）。进度与预估基于历史分布给范围，后台的完成 / 失败通知含部分结果与阻塞原因。审批三要素：为什么问（`justification`）、影响范围（改哪些文件、调哪个系统、可否撤销）、选项（允许一次 / 本会话这类 / 拒绝 / 看细节），超时即拒；只有允许 / 拒绝的弹窗训练用户点允许。diff 式审阅：代码 diff、文档修订标记、配置字段对比、数据行级对比，支持部分接受、评论、要求修改；整段替换无法核对。详见[第三章](#三进度步骤审批diff)。

[^q2]: 对话框成为默认只因它是最便宜的集成；局限：用户要会问（空白框把设计负担转给用户，边界外问题最易出现）、打断工作流（切窗口复制回来）、输出压成文本（表格图表表单动作丢失）、来回成本、不适合长任务（盯着等）；适合探索性、需来回、用户知道要问什么的。七种非对话形态：内联建议（高频小粒度在流中——补全、灰字）、按钮化动作（任务明确参数少——"总结" / "修复" / "起草回复"）、环境感知（在 IDE / 邮件 / 表格 / CRM / IM 里而非另一窗口）、生成式 UI（结构化输出渲染成表格 / 图表 / 表单 / 卡片，MCP Apps 是协议层版本）、语音与实时（免手、移动、电话）、后台交付（PR / 草稿 / 报告 / 通知）、环境自动（无界面事件触发，边界内可验证高量）；多数产品是组合。判据：用户在哪、任务多长、输出形状、要不要来回、参数是否已知、手眼是否空、输出要不要人看；纪律是先问"为什么是对话框"。详见[第四章](#四对话框的局限)到[第六章](#六选形态的判据)。
